import {
    USER_LOGIN,
    USER_LOGIN_SUCCESS,
    USER_LOGIN_FAILED,
} from '../types';

const INITIAL_STATE = {

};


export default (state = INITIAL_STATE, action) => {
    switch (action.type) {        
        default:
            return state;
    }
};
